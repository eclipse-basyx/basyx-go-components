server:
  port: 5004
  contextPath: "/"
  host: 0.0.0.0

postgres:
  host: localhost
  port: 5432
  user: admin
  password: admin123
  # dbname: basyxGoBenchmarkTest
  dbname: basyxTestDB
  maxOpenConnections: 500
  maxIdleConnections: 500
  connMaxLifetimeMinutes: 5

oidc:
  issuer: "http://localhost:8081/realms/basyx"
  audience: "discovery-service"
  jwksURL: ""  # optional, leave empty for auto-discovery

abac:
  enabled: true
  tenantClaim: "tenant"
  editorRole: "aas_editor"
  clientRolesAudience: "discovery-service"
  realmAdminRole: "admin"