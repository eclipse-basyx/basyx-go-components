server:
  port: 5004
  contextPath: "/"
  host: 0.0.0.0

postgres:
  host: db
  port: 5432
  user: admin
  password: admin123
  dbname: basyxTestDB
  maxOpenConnections: 500
  maxIdleConnections: 500
  connMaxLifetimeMinutes: 5

oidc:
  issuer: "http://keycloak.basyx.localhost:8080/realms/basyx"
  audience: "discovery-service"
  jwksURL: "" 

abac:
  enabled: true
  tenantClaim: "tenant"
  editorRole: "aas_editor"
  clientRolesAudience: "discovery-service"
  realmAdminRole: "admin"
  modelPath: "/config/access-rules.json"