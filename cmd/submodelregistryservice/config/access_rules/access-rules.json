{
  "AllAccessPermissionRules": {

    "rules": [
      {
        "ACL": {
          "ATTRIBUTES": [ { "GLOBAL": "ANONYMOUS" } ],
          "RIGHTS": ["READ"], 
          "ACCESS": "ALLOW" 
        },
        "OBJECTS": [ {"DESCRIPTOR": "$smdesc(\"*\")"} ],
        "FORMULA": { "$boolean": true }
      }
    ]
  }
}
