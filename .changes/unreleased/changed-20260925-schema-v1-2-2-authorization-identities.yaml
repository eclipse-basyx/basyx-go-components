kind: changed
body: 'Database schema v1.2.2 adds a persistent `auth_uuid` to `aas`, `submodel`, `concept_description`, `descriptor`, `aas_identifier` and `aasx_package`, and adds the `rebac_*` tables. Applying the patch rewrites these tables under an exclusive lock; plan a maintenance window for large installations and apply schema v1.2.2 with the configuration service before upgrading services.'
time: 2026-09-25T21:00:01Z
custom:
    Impact: High
    PullRequest: 712
    SecurityImpact: None.
