{
  "$condition": {
    "$contains": [
      {
        "$field": "$sme#idShort"
      },
      {
        "$strVal": "CollectionProperty"
      }
    ]
  },
  "$filters": [
    {
      "$fragment": "$sme.NewTestCollection.CollectionProperty#value",
      "$condition": {
        "$eq": [
          {
            "$field": "$sme.NewTestCollection.CollectionProperty#value"
          },
          {
            "$strVal": "collectionValue"
          }
        ]
      }
    }
  ]
}
