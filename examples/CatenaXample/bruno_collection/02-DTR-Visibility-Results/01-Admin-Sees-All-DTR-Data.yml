info:
  name: "Admin Sees All DTR Data"
  type: http
  seq: 1

http:
  method: GET
  url: "http://localhost:5004/api/v3/shell-descriptors"
  auth:
    type: none

runtime:
  scripts:
    - type: before-request
      code: |-
        const tokenVariable = "adminAccessToken";
        const expiresAtVariable = "adminAccessTokenExpiresAt";
        const nowSeconds = Math.floor(Date.now() / 1000);
        
        function tokenIsFresh() {
          const expiresAt = Number(bru.getVar(expiresAtVariable) || 0);
          return expiresAt - 30 > nowSeconds;
        }
        
        function formUrlEncode(fields) {
          return Object.entries(fields)
            .map(([key, value]) => `${encodeURIComponent(key)}=${encodeURIComponent(value)}`)
            .join("&");
        }
        
        async function requestOAuth2Token() {
          if (tokenIsFresh()) {
            return bru.getVar(tokenVariable);
          }
        
          const response = await bru.sendRequest({
            url: "http://keycloak.localhost:8080/realms/basyx/protocol/openid-connect/token",
            method: "POST",
            headers: {
              "Content-Type": "application/x-www-form-urlencoded",
            },
            data: formUrlEncode({
              grant_type: "password",
              client_id: "basyx-ui",
              username: "admin",
              password: "pwd",
            }),
          });
        
          const status = response.status || response.statusCode || response.code;
          if (status !== 200) {
            throw new Error(`OAuth2 token request failed with status ${status}`);
          }
        
          const tokenResponse = typeof response.data === "string" ? JSON.parse(response.data) : response.data;
          bru.setVar(tokenVariable, tokenResponse.access_token);
          bru.setVar(expiresAtVariable, String(nowSeconds + Number(tokenResponse.expires_in || 300)));
        
          return tokenResponse.access_token;
        }
        
        const token = await requestOAuth2Token();
        req.setHeader("Authorization", `Bearer ${token}`);
    - type: tests
      code: |-
        test('admin sees all descriptors, asset IDs, and markers', function () {
            expect(res.status).to.equal(200);
            const shell = res.body.result[0];
            expect(shell.idShort).to.eql('ProductTwin');
            expect(shell.specificAssetIds).to.have.length(2);
            expect(shell.submodelDescriptors).to.have.length(2);
            const customerKeys = shell.specificAssetIds.find((item) => item.name === 'customerPartId').externalSubjectId.keys.map((key) => key.value);
            expect(customerKeys).to.eql(['BPN_COMPANY_001', 'BPN_COMPANY_002']);
            const restrictedKeys = shell.submodelDescriptors.find((item) => item.idShort === 'RestrictedSerialPartTypization').supplementalSemanticId[0].keys.map((key) => key.value);
            expect(restrictedKeys).to.eql(['BPN_COMPANY_001', 'BPN_COMPANY_002']);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
