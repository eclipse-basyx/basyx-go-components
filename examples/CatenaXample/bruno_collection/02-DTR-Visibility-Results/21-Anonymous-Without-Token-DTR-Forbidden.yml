info:
  name: "Anonymous Without Token DTR Forbidden"
  type: http
  seq: 21

http:
  method: GET
  url: "http://localhost:5004/api/v3/shell-descriptors"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test('DTR request without token is forbidden', function () {
            expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
