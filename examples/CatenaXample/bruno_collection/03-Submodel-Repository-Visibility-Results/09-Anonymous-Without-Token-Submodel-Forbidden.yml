info:
  name: "Anonymous Without Token Submodel Forbidden"
  type: http
  seq: 9

http:
  method: GET
  url: "http://localhost:5005/submodels"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test('submodel request without token is forbidden', function () {
            expect(res.status).to.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
